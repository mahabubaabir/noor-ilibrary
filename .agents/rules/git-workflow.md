# Git Auto-Push Rule

After implementing any feature, bug fix, or modification requested by the user:
1. Always stage changes with `git add -A`.
2. Commit with a clear, descriptive message: `git commit -m "..."`.
3. Automatically push changes to the remote repository: `git push origin main`.
4. Verify the push succeeds before concluding the turn.
