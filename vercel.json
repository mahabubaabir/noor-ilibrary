{
  "version": 2,
  "installCommand": "npm install",
  "buildCommand": "npx prisma db push --schema=apps/web/prisma/schema.prisma && npx prisma generate --schema=apps/web/prisma/schema.prisma && npm run build",
  "outputDirectory": "apps/web/.next",
  "framework": "nextjs"
}
